{
  "name": "jlpt-vocab-game",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server -d web 8000",
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests -p 'test_*.py' -v && python3 tests/test_build_data.py",
    "build:data": "python3 build_web_data.py && python3 build_grammar_cloze.py && python3 build_grammar_order.py"
  }
}
